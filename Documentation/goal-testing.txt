HOME SCREEN MANAGER — TESTING TRACKER
=======================================================

Rule
----
Only record results observed on a real Jellyfin 10.11.11 server. A clean build
or source audit is not a live-server result.

Current dashboard checks
------------------------
- [x] Build the Release DLL with the exact Jellyfin 10.11.11 packages.
- [ ] Install the DLL in a dedicated Jellyfin plugin directory and restart the
      server without a plugin-load exception.
- [ ] Confirm Home Screen Manager appears in Dashboard navigation.
- [ ] Confirm its page is available only to an administrator.
- [ ] Confirm the header links open the correct repository and Patreon pages.
- [ ] Confirm all three tabs render and switch correctly.
- [ ] Confirm no JavaScript expressions or translation-token text appears in
      the Existing Home Screen Sections list or section-type picker.
- [ ] Confirm all eight Main Settings load, save, display Jellyfin's standard
      saved notification, and remain restored after the Dashboard page reloads.
- [ ] Confirm the Infinite Scroll picker lists every accessible user library and
      both Save buttons preserve the other Main Settings values.
- [ ] Confirm Customization Settings loads, saves, and applies the supported
      Abyss settings while preserving unrelated Custom CSS.
- [ ] Confirm current Jellyfin sections appear in their saved order with blue
      Jellyfin labels, every row is draggable, and only the first row receives
      the Media Bar label.
- [ ] Confirm only one section row highlights at a time and the Jellyfin, Home
      Screen Manager, and Media Bar label colors all save and restore after reload.
- [ ] Confirm Create Section inserts below the selected row, or at the bottom
      when no row is selected, with an editable inline Section Name field.
- [ ] Confirm plugin rows drag, edit, delete, and restore after a page reload.
- [ ] Exercise all fourteen section types and compare their reused pickers with the
      corresponding Collection Manager tabs.
- [ ] Confirm collection/tag/library choices show the expected Content In
      Section media and row modes show primary images.
- [ ] Confirm every image-based Content In Section preview shows 16 items per
      page with working Previous/Next controls, an unchanged large edit retains
      every saved ID, and the explicit manual-order action loads the full
      text-only draggable list only when requested.
- [ ] Throttle or hold the Jellyfin library and collection catalog requests,
      then confirm Select Content opens immediately, each catalog reports its
      own failure without losing the draft, and no saved-ID request contains
      more than 16 IDs or returns HTTP 400 through the remote reverse proxy.
- [ ] Confirm the collection, library, and metadata-tag source pickers for
      Rotating Sections and Seasonal Sections are all collapsed by default.
- [ ] Confirm each selected collection, library, or metadata tag creates one visible draft,
      draft cards show current matching-media counts, and removing or dragging a
      draft updates the saved draft list and order.
- [ ] Confirm rotation amount and hour/day/week units save and restore.
- [ ] Confirm Top 10-50 reuses the three collapsed Section Draft source pickers,
      combines every selected source into one editable draft, accepts common IMDb rating-tag formats or Jellyfin CommunityRating metadata, and saves Top 10/20/30/40/50.
- [ ] Confirm Top 10-50 numbering can be disabled or displayed with solid,
      vertical-gradient, horizontal-gradient, or center-gradient colors; verify
      the number overlaps the left side of each art card and the optional TTF/OTF
      font saves, restores, and renders.
- [ ] Confirm Other Users Activity supports Movies, Series, Music/Audiobooks,
      and Books, changes its default name appropriately, respects the maximum
      item count, randomizes recent/current results, and exposes no user identity.
- [ ] Confirm every seasonal draft's start/end month and day save and restore,
      including a date window that crosses from December into January.
- [ ] Confirm Save and Move On restores the saved selection when editing and
      reveals Section Content Order Settings.
- [ ] Confirm all nine content-order choices, including a new shuffle after every page reload, save and restore for every section
      type.
- [ ] Select Manual ordering and confirm every section item appears as a
      text-only draggable row, reorders correctly, and restores in that order.
- [ ] Confirm Section Art Appearance Settings appears after ordering and before
      Add Section to Home Screen.
- [ ] Confirm all five sizes, Poster plus all official Jellyfin image-type choices, all four shapes, and the
      Show text under the art checkbox save, restore, and render as selected.
- [ ] Confirm Add Section to Home Screen reports a clear dependency error when
      JavaScript Injector is not installed.
- [ ] Install JavaScript Injector and File Transformation 2.5.11.0 for Jellyfin
      10.11.11, restart, and add a section.
- [ ] Confirm Home-Screen-Manager-Loader is enabled in JavaScript Injector and
      every pre-existing Injector script remains unchanged.
- [ ] Confirm an applied section appears on Home with its configured media and
      selected content order.
- [ ] Confirm the credited plugin media bar retains Abyss's logo treatment,
      backdrop blending, metadata, buttons, animation, and responsive sizing.
- [ ] Confirm the original Continue Watching/artwork slide never flashes, the configured
      media-bar title and controls appear promptly from its matching saved cache,
      and no custom row waits for the media bar or an unrelated live refresh.
- [ ] Confirm saved custom-row snapshots appear first and remain usable while
      their collection, tag, library, rotation, seasonal, or activity rule
      refreshes in the background.
- [ ] Confirm Home, hearts, My List controls, native playback, and custom rows remain responsive while the media bar and bounded background refresh are loading.
- [ ] Click Refresh Home Screen Sections and confirm applied static sections are
      processed sequentially, one final save occurs, progress remains visible,
      and Home/playback on other tabs stays responsive.
- [ ] Confirm Series mode in Other Users Activity displays series cards rather than episode cards.
- [ ] Drag eligible native and plugin sections into the first position; confirm
      each one supplies the media-bar items and remains visible as the first normal row.
- [ ] Confirm My Media, My Media (small), and Latest Media cannot be dragged into
      the Media Bar position and are never selected as its runtime source.
- [ ] With Continue Watching selected, confirm the media bar can cycle through all
      available results returned by its 30-item request, not only the first few.
- [ ] Confirm 1–300 second timing and Backdrop, Primary, Banner, and Thumbnail
      image choices apply without changing the Abyss layout.
- [ ] Click every media-bar dot repeatedly and confirm the selected slide renders,
      autoplay continues, and changing image type cannot leave artwork from the
      previous image-type setting on screen.
- [ ] Confirm a Rotating Section changes to the next saved draft after its exact
      interval and preserves the shared content-order and art settings.
- [ ] Confirm a Seasonal Section shows the first matching draft during its
      inclusive recurring date window and hides when no draft window matches.
- [ ] Confirm collection and library content rows refresh their live children;
      after Collection Manager refreshes its metadata catalog, click Refresh
      Home Screen Sections and confirm saved tag rules gain newly matching media.
- [ ] Rename a library and confirm plugin library/tag sources remain attached by
      ID while every picker and label uses the new live name.
- [ ] Delete and re-add a library with the same name/type and confirm a stale
      Movies/TV/Music/Books link is repaired to the new current user-view ID,
      then use Refresh Home Screen Sections to persist the migrated source.
- [ ] With more than one library of the same type, confirm no ambiguous deleted
      library is silently mapped to the wrong replacement.
- [ ] Confirm every eligible live library has one Jellyfin-labeled Recently
      Added row that can be independently shown, hidden, and reordered.
- [ ] Rename, delete, and add libraries and confirm the Recently Added list uses
      current Jellyfin user views: stable-ID renames keep their position, deleted
      rows disappear without stale links, and new libraries receive fresh rows.
- [ ] Confirm the Home logo stays beside the hamburger menu on Home,
      libraries, search, settings, and breadcrumbed detail pages, always navigates
      Home, and is absent only from playback screens.
- [ ] Confirm Accent Color changes the shared Abyss hover accent on top
      navigation text, play/resume buttons, and sidebar menu items.
- [ ] Confirm the standalone Top Navigation Selected Text Color Settings section changes
      only the active top-navigation tab while it is not hovered; hover still uses
      the Accent Color.
- [ ] Confirm each split solid/gradient color changes only its named normal-state
      surface and unrelated Custom CSS remains intact.
- [ ] Confirm Continue Watching X resets progress without changing played state;
      confirm a dismissed Next Up card disappears without marking it played.
- [ ] Confirm My List appears inside the same native top slider as Home and
      Favorites, one heart changes Jellyfin Likes, the tab shows liked items, and
      unlike removes an item immediately.
- [ ] Like an episode and confirm My List keeps it as an episode entry while
      showing series art and the series name first, with the episode title beneath.
- [ ] Confirm series and season pages show correct counts and Ends at values.
- [ ] Confirm selected libraries append the next native page near the bottom and
      preserve the current Jellyfin sort, filters, card type, and scroll place.
- [ ] Confirm native and plugin-created rows respond to Jellyfin's desktop arrows, mouse
      wheel, mouse drag, touch, and focus navigation.
- [ ] Confirm Also Part of These Collections and enhanced Jellyfin-only search work.
- [ ] Confirm breadcrumbs promptly use Movies/Shows/Music media structure and
      sibling season, album, and song selectors rather than raw folder paths, then
      disappear immediately when returning Home.
- [ ] Confirm native Jellyfin rows remain present and follow the saved hybrid
      order.
- [ ] Confirm no Jellyfin Web source file is modified on disk.

Results log
-----------
No live-server results recorded yet.
